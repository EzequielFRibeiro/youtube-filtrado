const User = require('./User');
const Video = require('./Video');
const Comment = require('./Comment');
const { Playlist, PlaylistVideo } = require('./Playlist');
const ViewHistory = require('./ViewHistory');

module.exports = {
  User,
  Video,
  Comment,
  Playlist,
  PlaylistVideo,
  ViewHistory
};
